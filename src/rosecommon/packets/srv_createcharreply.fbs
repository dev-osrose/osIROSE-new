namespace RoseCommon;

enum CreateResult {
    OK = 0,
    FAILED,
    NAME_TAKEN,
    INVALID_VALUE,
    TOO_MANY_CHARS,
    BLOCKED
}

table SrvCreateCharReply {
    result: CreateResult;
    platinium: uint8;
}
