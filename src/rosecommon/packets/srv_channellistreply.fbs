namespace RoseCommon;

struct ChannelInfo {
    name: string;
    id: uint8;
    lowAge: uint8;
    highAge: uint8;
    capacity: uint16;
}

table SrvChannelListReply {
    id: uint32;
    channels: [ChannelInfo];
}
